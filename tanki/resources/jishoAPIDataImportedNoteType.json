{
    "name": "jishoAPIDataImportedNoteType",
    "fieldNames": ["jsonData", "userNotes"],
    "script": "const $that = this; function $(x) { return $that.querySelector(x); }",
    "cardTypes": [
        {
            "name": "Word -> Kana",
            "frontTemplate": "<h1 id=\"word\"></h1>",
            "frontScript": "$('#word').innerHTML = JSON.parse(jsonData).slug; f console.log(require);",
            "backTemplate": "{{jsonData}}, {{userNotes}}"
        }, {
            "name": "Word -> Meaning",
            "frontTemplate": "{{jsonData}}",
            "backTemplate": "{{jsonData}}, {{userNotes}}"
        }, {
            "name": "Kana + Meaning -> Word",
            "frontTemplate": "{{jsonData}}",
            "backTemplate": "{{jsonData}}, {{userNotes}}"
        }
    ]
}
